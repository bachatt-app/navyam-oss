{
  "_comment": "Embedding-transfer proxy experiment (de-risking v0.3-64k -> v0.5-B96k). navya-0 shape (58M) warm-started via --init-from from an FVT-transferred embedding (code/01-tokenizer/embedding_transfer.py), then a short continued-pretrain on a small re-tokenized finance-domain slice. Compare against proxy-v05-random.json (same run, random-init embedding) to show the transfer head start.",
  "vocab_size": 96000,
  "dim": 512,
  "n_layers": 8,
  "n_heads": 8,
  "n_kv_heads": 2,
  "max_seq_len": 1024,
  "data_dir": "data/proxy_v05_corpus",
  "out_dir": "out/proxy-v05-transfer",
  "batch_size": 16,
  "grad_accum": 1,
  "max_steps": 500,
  "lr": 2e-4,
  "min_lr_frac": 0.1,
  "warmup_steps": 20,
  "weight_decay": 0.1,
  "log_interval": 10,
  "eval_interval": 50,
  "eval_iters": 20,
  "ckpt_interval": 500,
  "peak_flops": 312000000000000.0
}
