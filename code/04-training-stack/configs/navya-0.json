{
  "_comment": "navya-0 \u2014 the first model (50M shape, BachattLM-0 in older docs). Budget: one pass over the ~1.3B-token navya0_v1 stream on the A10 (~5-6h, ~$18). Micro-batch kept small: 64K-vocab logits OOM above 8x1024 on 24GB; chunked CE is the stack TODO.",
  "vocab_size": 65536,
  "dim": 512,
  "n_layers": 8,
  "n_heads": 8,
  "n_kv_heads": 2,
  "max_seq_len": 1024,
  "data_dir": "data/navya0_v1",
  "out_dir": "out/navya-0",
  "batch_size": 8,
  "grad_accum": 16,
  "max_steps": 10000,
  "lr": 0.0006,
  "warmup_steps": 300,
  "weight_decay": 0.1,
  "log_interval": 20,
  "eval_interval": 500,
  "eval_iters": 100,
  "ckpt_interval": 250,
  "peak_flops": 125000000000000.0
}