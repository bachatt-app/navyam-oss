{
  "_comment": "BachattLM-0 target shape (~59M params w/ 64K vocab embed). A10: ~1hr per 500M tokens. vocab_size overridden by data meta.json. peak_flops = A10 BF16 dense. Micro-batch kept small: 64K-vocab logits [B*T,V] OOM the 24GB card above ~8x1024; chunked cross-entropy is the real fix (stack TODO).",
  "vocab_size": 65536,
  "dim": 512,
  "n_layers": 8,
  "n_heads": 8,
  "n_kv_heads": 2,
  "max_seq_len": 1024,
  "data_dir": "data/real_v0",
  "out_dir": "out/bachattlm0-50m",
  "batch_size": 8,
  "grad_accum": 16,
  "max_steps": 4000,
  "lr": 0.0006,
  "warmup_steps": 200,
  "weight_decay": 0.1,
  "log_interval": 20,
  "eval_interval": 500,
  "eval_iters": 50,
  "ckpt_interval": 500,
  "peak_flops": 125000000000000.0
}